import Button from "./components/Button";

const App = () => {

  const handleOnClick = () => {

  }

  return (
    <div>
      <Button onClick={handleOnClick} color="">
        My Button
      </Button>
    </div>
  );
};

export default App;
