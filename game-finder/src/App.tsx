import Button from "./components/Button";

const App = () => {

  const handleOnClick = () => {

  }

  return (
    <div>
      <Button onClick={handleOnClick}>
        My Button
      </Button>
    </div>
  );
};

export default App;
